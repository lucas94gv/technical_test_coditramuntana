<%= form_with(model: lp) do |form| %>
  <% if lp.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(lp.errors.count, "error") %> prohibited this lp from being saved:</h2>

      <ul>
        <% lp.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :artist_id, style: "display: block" %>
    <%#= form.text_field :description %>
    <%= form.select :artist_id, Artist.order(:name).map { |artist| [artist.name, artist.id] } %>
  </div>


  <div>
    <%= form.submit %>
  </div>
<% end %>
